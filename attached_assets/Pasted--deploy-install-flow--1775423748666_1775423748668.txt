Дополнительно внеси все исправления не только в код проекта, но и в deploy/install flow и документацию.

Нужно, чтобы все новые исправления были отражены в:

1. deploy/install.sh
2. deploy/update.sh
3. README.md
4. README_DEPLOY.md
5. .env.example
6. если нужно — в отдельных deploy helper scripts

Что именно нужно включить в install/deploy и документацию:

1. Авторизация / сессии
- если были исправлены проблемы с истечением сессии, это должно быть отражено в install/deploy flow
- описать, как работает superadmin bootstrap
- описать, какие env-переменные нужны для авторизации и bootstrap

2. SMTP / почта
- если исправлена отправка почты, нужно:
  - добавить нужные SMTP env-переменные в `.env.example`
  - описать их в README / README_DEPLOY
  - указать, как настраивать Mail.ru SMTP
  - указать, что нужен пароль для внешнего приложения
- install script должен учитывать, что SMTP может быть включен сразу
- если есть тест SMTP или проверка транспорта, это тоже должно быть отражено

3. Уведомления
- если исправлены уведомления о новых чатах / новых сообщениях / заказах / заявках, нужно описать это в README
- если есть дополнительные env или настройки для уведомлений, они должны быть в `.env.example`

4. PWA / push
- если исправлены PWA install и push-уведомления:
  - описать это в README / README_DEPLOY
  - добавить нужные env-переменные в `.env.example`
  - указать, как генерируются и используются VAPID keys
  - указать, как работает подписка на push
- install flow должен учитывать, если для push нужны отдельные настройки

5. robots.txt / sitemap / feeds
- если добавлены robots.txt, sitemap.xml, yml/xml feed:
  - описать это в README
  - указать endpoint’ы
  - указать, как они формируются
  - если есть флаги/настройки, внести их в `.env.example`

6. Demo seed / test data
- если demo seed доработан:
  - install.sh должен уметь это запускать корректно
  - README должен описывать, как включить demo seed
  - `.env.example` должен содержать:
    - APP_RUN_DEMO_SEED
    - BOOTSTRAP_DEMO_PRODUCTS
    - BOOTSTRAP_DEMO_TOURS
    - BOOTSTRAP_DEMO_BRANCHES
    - BOOTSTRAP_DEMO_SETTINGS
- install script не должен “продолжать на авось”, если API еще не готов

7. Superadmin bootstrap
- install.sh должен:
  - реально дожидаться API
  - создавать superadmin
  - инициализировать settings
  - запускать demo seed
- в финале install script должен явно печатать:
  - email администратора
  - временный пароль
  - рекомендацию сменить пароль после первого входа

8. Обновление проекта
- если исправляется update flow, это тоже нужно отразить в `deploy/update.sh`
- README / README_DEPLOY должны содержать понятную инструкцию:
  - как обновлять проект из GitHub
  - как выполнять миграции
  - как пересобирать контейнеры
  - как не потерять данные

9. Nginx / SSL / certbot
- если исправлены nginx, /crm, /api, SSL flow:
  - это нужно внести в README / README_DEPLOY
  - описать:
    - first install без SSL
    - включение SSL после сертификатов
    - какие файлы нужны
    - как копируются certs
    - как включается HTTPS
- install flow должен быть согласован с реальным nginx конфигом

10. Формат результата
В ответе нужно показать:
1. какие изменения внесены в install.sh
2. какие изменения внесены в update.sh
3. что добавлено или исправлено в README.md
4. что добавлено или исправлено в README_DEPLOY.md
5. что добавлено в `.env.example`
6. какие новые шаги установки и обновления теперь поддерживаются
7. как это протестировано

Нужно не просто “исправить код”, а сделать так, чтобы все работало системно через install/deploy и было задокументировано.