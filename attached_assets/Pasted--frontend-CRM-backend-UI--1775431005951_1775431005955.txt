Исправь frontend CRM: backend уведомлений уже работает, но UI не показывает количество уведомлений и badge.

Что уже подтверждено:
1. backend исправен
2. API отдает рабочие данные
3. проверенные endpoints:
- GET /api/notifications/counts → возвращает counts, например:
  {"newOrders":2,"newSaleOrders":0,"pendingBookings":0,"unreadChats":1}
- GET /api/notifications/recent → возвращает реальные уведомления
- GET /api/chat/sessions/unread → возвращает unread count, например:
  {"unread":2}

Значит проблема именно во frontend/admin UI.

Что нужно исправить:

1. Подключить counts и unread во frontend CRM
- использовать:
  - /api/notifications/counts
  - /api/notifications/recent
  - /api/chat/sessions/unread
- вывести реальные счетчики в UI

2. Показать badge / counters
Нужно, чтобы в CRM появились:
- общий блок уведомлений
- счетчик новых уведомлений
- счетчик непрочитанных чатов
- счетчик новых заказов
- визуальные badge у соответствующих разделов

3. Показать список недавних уведомлений
- использовать `/api/notifications/recent`
- отобразить список новых событий:
  - новые сообщения
  - новые чаты
  - новые заказы
- сделать нормальный UI списка уведомлений

4. Обновлять счетчики автоматически
- без ручной перезагрузки страницы
- через polling или websocket
- если polling, сделать разумный интервал
- после открытия нужного раздела счетчики должны обновляться

5. Сделать корректное поведение unread
- если открыт чат и он прочитан, unread count должен уменьшаться
- если пришло новое сообщение, unread count должен увеличиваться
- если просмотрено уведомление, UI должен обновляться

6. Проверить, почему сейчас фронт не показывает данные
Нужно найти реальную причину:
- endpoint вообще не вызывается
- вызов есть, но ответ не используется
- компонент badge не подключен
- данные не пробрасываются в layout/header/sidebar
- ошибка в mapping / typings / state management

7. В ответе показать:
1. какие frontend-файлы изменены
2. почему UI раньше не показывал counts
3. как теперь подключены endpoints уведомлений
4. где теперь отображаются badge и список уведомлений
5. как реализовано автообновление
6. какие проверки проведены

Важно:
backend уже работает, проблему нужно искать и исправлять именно во frontend CRM.