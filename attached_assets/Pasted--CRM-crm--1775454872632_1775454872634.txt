Исправь проблему с роутингом CRM: сейчас при переходах внутри админки `/crm` пользователя выбрасывает на 404.

Проблема:
- CRM может открываться на `/crm/`
- но при переходах внутри админки, а также при прямом открытии внутренних URL, появляются 404
- ломаются маршруты вроде:
  - `/crm/chat`
  - `/crm/settings`
  - `/crm/admin`
  - другие вложенные страницы CRM
- также может ломаться обновление страницы на внутренних маршрутах CRM

Нужно полностью исправить routing для CRM.

Что нужно проверить и исправить:

1. Nginx reverse proxy для `/crm/`
- проверить блок:
  - `location = /crm`
  - `location /crm/`
- проверить trailing slash в `proxy_pass`
- убедиться, что `/crm` редиректит на `/crm/`
- убедиться, что `/crm/...` корректно проксируется в admin container

2. Конфиг admin container
- проверить внутренний nginx внутри admin
- убедиться, что это SPA fallback
- внутри admin container routes должны отдавать `index.html`, а не 404
- нужно проверить `try_files` и fallback

3. Frontend CRM base path
- проверить, как собрана админка
- если используется React Router, проверить `basename="/crm"`
- если используется Vite, проверить `base: '/crm/'`
- проверить все asset paths
- убедиться, что js/css/chunks/assets грузятся с правильного пути `/crm/...`, если это нужно по сборке

4. Поведение прямых URL
Нужно, чтобы работало:
- открытие `/crm/`
- открытие `/crm/chat`
- открытие `/crm/settings`
- открытие `/crm/admin`
- обновление страницы на любом внутреннем маршруте CRM
- переходы внутри SPA без 404

5. Проверить service worker / PWA для CRM
- если у CRM есть свой service worker, проверить, что он не ломает маршрутизацию
- не должно быть конфликтов между `/sw.js`, `/crm/sw.js`, fallback и offline handling

6. Что нужно получить в итоге
- CRM работает стабильно на префиксе `/crm`
- любые внутренние маршруты CRM открываются без 404
- перезагрузка страницы на внутренних маршрутах тоже работает
- переходы внутри CRM не выбрасывают на 404

Формат ответа:
1. в чем была причина 404
2. какие файлы изменены
3. как исправлен nginx reverse proxy
4. как исправлен admin container fallback
5. как исправлен frontend base path / router basename
6. какие маршруты были протестированы