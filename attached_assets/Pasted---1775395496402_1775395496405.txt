Исправь критичную ошибку в модуле туров: при добавлении даты бронирования тура и при смене статуса возникает `Internal Server Error`. Это нужно не обходить, а найти реальную причину и исправить системно.

==================================================
1. ПРОБЛЕМА
==================================================

Сейчас в модуле туров возникают ошибки:
- при создании даты тура
- при изменении статуса даты тура
- возможно при редактировании tour date

Ошибка:
- Internal Server Error

Это критично, потому что без этого нельзя нормально управлять расписанием туров.

==================================================
2. ЧТО НУЖНО ПРОВЕРИТЬ
==================================================

Проверь весь flow для `tour_dates`:

1. frontend form create tour date
2. frontend edit/status change form
3. request payload
4. backend DTO / validation
5. controller
6. service layer
7. DB insert/update
8. поля:
   - tourId
   - instructorId
   - startDate
   - endDate
   - seatsTotal
   - seatsBooked
   - price
   - depositAmount
   - status
   - notes
9. enum/status handling
10. timestamp/date parsing
11. null/undefined handling
12. relations with tours/users
13. default values
14. migrations/schema mismatch

Особенно проверь:
- не ломается ли `status`
- не ломается ли `startDate/endDate`
- не ломается ли `price/depositAmount`
- не ломается ли `instructorId`
- не ломается ли `tourId`

==================================================
3. НУЖНО НАЙТИ ROOT CAUSE INTERNAL SERVER ERROR
==================================================

Не надо просто оборачивать все try/catch и скрывать ошибку.

Нужно:
1. найти точную причину 500
2. показать root cause
3. исправить backend
4. исправить frontend payload, если он неверный
5. исправить validation/schema, если они не совпадают
6. логировать понятную ошибку в system logs

Если проблема в:
- enum
- nullable fields
- дате
- decimal/numeric parsing
- relation constraint
- wrong field naming
то это надо явно исправить

==================================================
4. СОЗДАНИЕ ДАТЫ ТУРА ДОЛЖНО РАБОТАТЬ СТАБИЛЬНО
==================================================

После исправления нужно, чтобы работал сценарий:

1. открыть тур в CRM
2. нажать “Добавить дату”
3. заполнить:
   - дату начала
   - дату окончания
   - цену
   - депозит
   - количество мест
   - инструктора
   - статус
4. сохранить
5. дата появляется в списке
6. дата доступна на сайте, если активна и положено по логике

==================================================
5. СМЕНА СТАТУСА ДАТЫ ТУРА ДОЛЖНА РАБОТАТЬ
==================================================

Сейчас смена статуса тоже ломается.

Нужно, чтобы можно было менять статус даты тура:
- planned
- active
- completed
- cancelled

И чтобы:
- статус сохранялся в БД
- UI обновлялся
- не было 500
- если есть связанные бронирования, логика не ломалась

Если для некоторых статусов нужны ограничения:
- реализуй понятную проверку
- но не Internal Server Error

==================================================
6. ПРОВЕРИТЬ CREATE / EDIT / STATUS UPDATE ПОЛНОСТЬЮ
==================================================

Проверь три сценария:
1. create tour date
2. edit tour date
3. change tour date status

Для каждого:
- request
- response
- DB update
- UI refresh
- error handling

==================================================
7. ЛОГИРОВАНИЕ
==================================================

Добавь понятное логирование для tour_dates ошибок:
- create failure
- update failure
- status change failure
- validation failure

Если ошибка повторится, по логам должно быть сразу видно:
- какой payload
- какое поле упало
- какой constraint/validation не прошел

==================================================
8. ЧТО НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС
==================================================

1. Найти root cause Internal Server Error при создании даты тура
2. Найти root cause Internal Server Error при смене статуса даты тура
3. Исправить backend
4. Исправить frontend payload/form, если нужно
5. Исправить validation/schema mismatch, если есть
6. Проверить create/edit/status update flow
7. Показать, какие файлы изменены
8. Показать, какие routes/services/components/schema changes внесены
9. Показать, как протестировано вручную

==================================================
9. ФОРМАТ ОТВЕТА
==================================================

Покажи:
1. Почему возникал Internal Server Error
2. Что именно было сломано в create tour date
3. Что именно было сломано в status change
4. Как это исправлено
5. Какие файлы изменены
6. Какие ручные тесты проведены

Не делай заглушки.
Сначала root cause audit, потом исправление.