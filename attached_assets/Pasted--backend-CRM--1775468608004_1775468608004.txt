Исправь backend раздела отзывов и доработай CRM по карточке клиента и заказам.

Уже подтверждено по логам API:
1. `GET /api/content/reviews/admin` падает с 500
2. `POST /api/content/reviews` падает с 500

Точные ошибки из логов:
- `column "author_last_name" does not exist`
- `TypeError: value.toISOString is not a function`

Это значит:
- схема БД `reviews` не соответствует текущему backend-коду
- backend пытается читать и писать колонку `author_last_name`, которой нет в таблице
- дополнительно сломана обработка `review_date`

Что нужно сделать по отзывам:
1. синхронизировать схему БД с текущей моделью `reviews`
2. добавить недостающие колонки в таблицу `reviews`, минимум:
   - `author_last_name`
   - и все остальные новые поля, если они есть в актуальной drizzle/schema модели
3. исправить обработку `review_date`
   - не вызывать `toISOString()` на строке
   - корректно приводить значение к `Date`, если нужно
   - или хранить/обрабатывать значение в соответствии с типом схемы
4. убедиться, что снова работают:
   - `GET /api/content/reviews/admin`
   - `POST /api/content/reviews`
   - `PATCH /api/content/reviews/:id`
   - `DELETE /api/content/reviews/:id`
   - `PATCH /api/content/reviews/:id/approve`
5. если проблема из-за расхождения drizzle schema и базы, привести их в консистентное состояние

Дополнительно по CRM и клиентам:
Нужно, чтобы в разделе "Клиенты" была полноценная возможность открыть карточку клиента и посмотреть всю историю клиента в одном месте.

Что нужно сделать по карточке клиента:
1. в разделе `Клиенты` должна быть рабочая кнопка / ссылка / действие:
   - `Открыть`
   - `Подробнее`
   - `Карточка клиента`
2. из списка клиентов должен открываться отдельный экран карточки клиента
3. карточка клиента должна быть доступна именно из раздела `Клиенты`, а не только косвенно из других сущностей
4. в карточке клиента должна отображаться полная история клиента:
   - аренды
   - покупки
   - туры
   - бронирования
   - заказы
   - заявки
   - отзывы
   - чаты
   - сообщения / история переписки
5. если есть чаты клиента, в карточке клиента должен быть отдельный блок чатов:
   - список диалогов
   - дата последнего сообщения
   - количество сообщений
   - статус
   - ссылка открыть чат
6. если у клиента нет данных в каком-то разделе, показывать пустое состояние, а не ломать интерфейс

Дополнительно по заказам:
Нужно проверить отображение точки самовывоза в заказе.

Сейчас проблема:
- в заказе просто написано `самовывоз`
- но не указывается конкретная точка самовывоза

Что нужно сделать:
1. проверить, как в заказе сохраняется способ получения
2. проверить, сохраняется ли конкретная точка самовывоза в базе
3. проверить frontend и backend заказа:
   - оформление заказа
   - сохранение заказа
   - отображение заказа в CRM
   - отображение заказа клиенту, если есть
4. если клиент выбрал самовывоз, в заказе должно быть видно:
   - что это самовывоз
   - из какой именно точки самовывоза
   - адрес / название точки / филиал
5. если сейчас точка самовывоза не сохраняется, нужно исправить это
6. если точка сохраняется, но не отображается, нужно исправить вывод
7. проверить это для:
   - аренды
   - покупки
   - других заказов, если там тоже есть самовывоз

Особенно важно:
- карточка клиента должна реально открываться из списка клиентов
- раздел `Клиенты` должен стать точкой входа в полную историю клиента
- вся история клиента должна быть собрана в одном месте
- в заказе самовывоз должен содержать конкретную точку, а не просто слово `самовывоз`

В ответе показать:
1. какие именно колонки отсутствовали в `reviews`
2. какие миграции / SQL / schema changes были применены
3. как исправлена обработка `review_date`
4. какие файлы изменены по отзывам
5. какие файлы изменены по карточке клиента
6. как теперь открывается карточка клиента из раздела `Клиенты`
7. какие сущности теперь видны в карточке клиента
8. как исправлено отображение точки самовывоза в заказах
9. какие endpoints и пользовательские сценарии протестированы