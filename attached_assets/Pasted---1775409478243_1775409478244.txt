Проведи полный аудит проекта и доведи его до состояния, в котором он с нуля нормально разворачивается на обычном Linux/VPS без ручных костылей. Нужно не точечно чинить отдельные ошибки, а системно закрыть весь production/deploy flow, который уже ломался при реальной установке.

Контекст проблем, которые уже проявились:
- bash/sh несовместимость в deploy scripts
- Redis readiness check был сломан из-за requirepass
- Redis падал при пустом REDIS_PASSWORD
- миграции падали, потому что drizzle-kit отсутствовал в контейнере
- install script использовал wget, которого нет в api container
- superadmin не создавался автоматически
- initial settings seed не выполнялся
- nginx падал, если SSL-конфиг включен до появления сертификатов
- certbot/webroot flow был не доведен
- /api и /crm ломались из-за неправильного proxy_pass
- /crm уходил в бесконечный redirect loop из-за внутреннего admin nginx
- demo/test данные в БД не наполнились
- не хватает шаблонов тарифов, которые можно создавать и потом выбирать для товаров

Нужно исправить это комплексно.

==================================================
1. DEPLOY И INSTALL FLOW ДОЛЖНЫ РАБОТАТЬ С НУЛЯ
==================================================

Нужен полностью рабочий сценарий развертывания на Linux/VPS:

1. скопировать проект
2. создать `.env` из `.env.example`
3. заполнить переменные
4. запустить:
   `bash deploy/install.sh`
5. получить:
   - postgres
   - redis
   - api
   - web
   - admin
   - nginx
   - миграции
   - seed
   - superadmin
   - demo data, если включено

То есть install flow должен реально работать без ручного добивания команд после падений.

==================================================
2. ИСПРАВИТЬ DEPLOY SCRIPTS ПОД BASH
==================================================

Проверить и привести в порядок:
- deploy/install.sh
- deploy/update.sh
- deploy/init-db.sh
- deploy/backup-db.sh
- deploy/restore-db.sh
- deploy/healthcheck.sh, если есть

Требования:
- везде `#!/usr/bin/env bash`
- никаких инструкций с `sh deploy/install.sh`
- в README и README_DEPLOY использовать только:
  `bash deploy/install.sh`

==================================================
3. REDIS ДОЛЖЕН РАБОТАТЬ КОРРЕКТНО
==================================================

Исправить весь Redis flow:

1. REDIS_PASSWORD должен быть обязательным для production
2. docker-compose redis service не должен падать, если env пустой
3. readiness check в install.sh должен работать с паролем
4. Redis healthcheck должен учитывать auth
5. install.sh не должен писать “Redis not ready”, если Redis реально жив

Проверить:
- docker-compose.yml
- redis command
- redis healthcheck
- install.sh Redis wait logic
- .env.example

==================================================
4. МИГРАЦИИ ДОЛЖНЫ РЕАЛЬНО ЗАПУСКАТЬСЯ В DOCKER
==================================================

Сейчас миграции падали, потому что:
- `drizzle-kit` не был доступен в контейнере, из которого запускаются миграции

Нужно исправить системно:

1. проверить Dockerfile.api
2. проверить `lib/db/package.json`
3. проверить install flow миграций
4. обеспечить, чтобы команда миграций реально работала внутри docker окружения
5. не требовать ручной `pnpm install` на хосте

Если нужно:
- сделать отдельный migrator service
или
- сделать migration-capable api image

Но итог должен быть такой:
- на чистом сервере install script сам запускает миграции без ручных действий

==================================================
5. INSTALL SCRIPT НЕ ДОЛЖЕН ИСПОЛЬЗОВАТЬ WGET ВНУТРИ API CONTAINER
==================================================

Сейчас superadmin setup и settings seed падали, потому что в api container нет wget.

Нужно:
1. заменить все внутренние HTTP вызовы в install.sh
2. не использовать wget/curl как обязательный бинарь внутри api container
3. использовать:
   - node fetch
   или
   - другой надежный встроенный способ
4. проверить:
   - API health check
   - setup-superadmin
   - auth/login
   - settings/init

==================================================
6. SUPERADMIN ДОЛЖЕН СОЗДАВАТЬСЯ АВТОМАТИЧЕСКИ
==================================================

Нужно довести до конца bootstrap superadmin:

Использовать env:
- BOOTSTRAP_SUPERADMIN_CREATE
- BOOTSTRAP_SUPERADMIN_EMAIL
- BOOTSTRAP_SUPERADMIN_PASSWORD
- BOOTSTRAP_SUPERADMIN_FIRST_NAME
- BOOTSTRAP_SUPERADMIN_LAST_NAME
- BOOTSTRAP_SUPERADMIN_PHONE

Требования:
- если включено, superadmin создается автоматически
- если уже существует, дубль не создается
- install script не падает из-за этого
- логика реально проверена на чистой БД

==================================================
7. INITIAL SETTINGS И SEED ДОЛЖНЫ РАБОТАТЬ
==================================================

Сейчас initial settings seed не отработал нормально.

Нужно:
1. проверить endpoint/settings init flow
2. исправить install script
3. убедиться, что после первого запуска базовые настройки реально появляются
4. если нужна авторизация — install script должен ее проходить корректно
5. не должно быть шага “сделайте вручную после входа”, если это можно автоматизировать

==================================================
8. НУЖНО РЕАЛЬНОЕ TEST/DEMO НАПОЛНЕНИЕ БД
==================================================

Сейчас даже после установки тестовые данные не появились.

Нужно реализовать и проверить полноценный demo seed.

Через `.env` должны поддерживаться флаги типа:
- APP_FIRST_INSTALL=true|false
- APP_RUN_MIGRATIONS=true|false
- APP_RUN_SEED=true|false
- APP_RUN_DEMO_SEED=true|false
- BOOTSTRAP_DEMO_PRODUCTS=true|false
- BOOTSTRAP_DEMO_TOURS=true|false
- BOOTSTRAP_DEMO_BRANCHES=true|false
- BOOTSTRAP_DEMO_SETTINGS=true|false

Если demo seed включен, система должна создавать тестовые данные.

Нужно наполнить хотя бы:
- товары аренды
- товары продажи
- категории
- туры
- даты туров
- филиалы / пункты самовывоза
- базовые настройки
- контакты
- отзывы/контент, если это уже предусмотрено
- клиентов/заказы — опционально, если уместно
- шаблоны тарифов
- шаблоны характеристик, если уже внедрены

Тестовое наполнение не должно быть пустым или формальным.

==================================================
9. ШАБЛОНЫ ТАРИФОВ ДОЛЖНЫ БЫТЬ ПОЛНОЦЕННОЙ СУЩНОСТЬЮ
==================================================

Нужно не просто один базовый шаблон, а полноценный механизм шаблонов тарифов.

Нужно:
1. отдельный раздел шаблонов тарифов в CRM
2. возможность создавать сколько угодно шаблонов
3. возможность редактировать шаблоны
4. возможность удалять шаблоны
5. возможность выбирать шаблон при работе с товаром
6. возможность применить шаблон к товару
7. после применения шаблон должен подставлять тарифы в карточку товара
8. при необходимости тарифы товара можно донастроить вручную

То есть логика должна быть:
- сначала создаем много шаблонов тарифов
- потом в карточке товара выбираем нужный

Проверить и для:
- аренды
- если нужно, для туров или других сущностей, где есть pricing templates

==================================================
10. NGINX И REVERSE PROXY ДОЛЖНЫ БЫТЬ ДОВЕДЕНЫ ДО КОНЦА
==================================================

Исправить production nginx flow полностью.

Проблемы, которые уже были:
- nginx падал без сертификатов
- certbot/webroot был не доведен
- /api ломался из-за proxy_pass
- /crm уходил в loop
- admin container имел внутренний redirect loop

Нужно:
1. проверить `docker/nginx.conf`
2. проверить `docker/nginx-admin.conf` или внутренний admin nginx config
3. убрать внутренние redirect loops
4. правильно проксировать:
   - `/`
   - `/crm`
   - `/api`
   - `/uploads`
5. поддержать `.well-known/acme-challenge`
6. сделать рабочую схему:
   - HTTP only для первичного certbot
   - HTTPS после получения сертификата
или
   - один production flow, который не падает без готовых cert файлов

Нужно, чтобы:
- hakdev.online работал
- /crm работал без redirect loop
- /api работал корректно
- www/hakdev.online были согласованы
- SSL setup был понятен и работал

==================================================
11. README И README_DEPLOY ДОЛЖНЫ БЫТЬ ПЕРЕПИСАНЫ ПОД РЕАЛЬНЫЙ FLOW
==================================================

Сейчас документация должна соответствовать реальному working flow, а не старым попыткам.

Нужно обновить:
- README.md
- README_DEPLOY.md
- .env.example

Обязательно описать:
1. как запускать install
2. какие env обязательны
3. как включить first install
4. как включить demo seed
5. как создается superadmin
6. как настраивается SSL
7. как работают update/backup/restore scripts
8. как проверить health
9. какие команды использовать на Linux/VPS

==================================================
12. НУЖЕН ПОЛНЫЙ FIRST INSTALL / UPDATE / RECOVERY FLOW
==================================================

Нужно проверить и довести:
- first install
- reinstall
- update after git pull
- backup / restore
- restart without data loss

Install script не должен быть одноразовым и хрупким.

==================================================
13. ЧТО НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС
==================================================

1. Провести аудит deploy/install flow
2. Исправить Docker migration flow
3. Исправить Redis flow
4. Исправить superadmin bootstrap
5. Исправить settings seed
6. Реализовать demo/test seed наполнения БД
7. Добавить полноценные шаблоны тарифов:
   - create/edit/delete/list/apply/select
8. Проверить шаблоны характеристик, если они уже внедрены
9. Исправить nginx/admin reverse proxy loop
10. Довести SSL/certbot flow
11. Обновить README, README_DEPLOY и .env.example
12. Протестировать все это на чистом запуске

==================================================
14. ФОРМАТ ОТВЕТА
==================================================

Покажи:
1. Какие корневые причины были найдены по deploy/install
2. Какие файлы изменены
3. Как исправлен Docker migration flow
4. Как исправлен Redis flow
5. Как теперь создается superadmin
6. Как теперь работает initial settings seed
7. Как теперь работает demo/test seed
8. Какие данные теперь создаются автоматически при demo seed
9. Как теперь устроены шаблоны тарифов
10. Где они создаются и как выбираются
11. Как исправлен /crm redirect loop
12. Как теперь выглядит рабочий сценарий установки на VPS
13. Какие ручные тесты проведены

Не отвечай общими словами.
Сначала проведи полный аудит, потом системно исправь проект.